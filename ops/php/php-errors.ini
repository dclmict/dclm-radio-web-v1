; PHP overrides for local development
; Mounted into the container to override base image production defaults.

; Error logging — show everything in dev
log_errors=On
error_log=/dev/stderr
error_reporting=E_ALL
display_errors=On

; Sessions — use /tmp with relaxed permissions
session.save_path=/tmp
session.save_handler=files
